# Distribuzioni di variabili casuali continue {#sec-prob-cont-distr}

## Introduzione {.unnumbered .unlisted}

Nel @sec-prob-discr-distr abbiamo considerato variabili con valori numerabili: risposte corrette/errate, numero di successi, conteggi. Qui passiamo alle **variabili casuali continue**, che si muovono su una scala numerica: punteggi psicometrici, tempi di risposta, durate, tassi, proporzioni, effetti medi e differenze fra gruppi.

La differenza chiave è che nel continuo la probabilità non si assegna ai singoli valori, ma agli  **intervalli**. Per $X$ è continua, 

$$
P(X = x) = 0,
$$
ha senso, invece,

$$
P(a \leq X \leq b).
$$
data dall'**area sotto la curva di densità** tra $a$ e $b$.

Restiamo qui in un'ottica bayesiana, ma senza svolgere ancora l'inferenza completa. Come nel capitolo precedente, l'obiettivo è riconoscere le principali famiglie di modelli generativi continui e il loro ruolo nel ragionamento probabilistico.

Una distribuzione continua può svolgere almeno tre ruoli:  

1. - modello dei dati osservabili: per esempio, normale per gli errori di misura, gamma per i tempi positivi e asimmetrici;  
2. - prior per parametri continui: per esempio, beta per una probabilità, gamma per un tasso, normale per un effetto;  
3. - componente di verosimiglianza: quando, dopo l'osservazione, lo stesso modello serve a individuare quali valori del parametro rendano i dati più plausibili.  

In questo capitolo ci concentreremo soprattutto sui primi due; il terzo sarà formalizzato nel @sec-prob-likelihood, dopo il capitolo-ponte @sec-prob-inference-uncertainty.

In questo capitolo:

1. **chiariremo** il significato di densita, supporto e probabilita come area;
2. **richiameremo** le funzioni R per lavorare con distribuzioni continue;
3. **studieremo** le distribuzioni uniforme, normale, esponenziale e gamma;
4. **introdurremo** la distribuzione beta come modello naturale per probabilita e proporzioni;
5. **discuteremo** le distribuzioni chi-quadrato, $t$ di Student e Cauchy come distribuzioni derivate o robuste;
6. **collegheremo** ogni distribuzione al suo ruolo nel percorso bayesiano complessivo.

::: {.callout-tip collapse="true"}
## Prerequisiti

Per seguire questo capitolo e utile aver letto:

- @sec-prob-random-var;
- @sec-prob-distr;
- @sec-prob-random-var-properties;
- @sec-prob-intro-distr;
- @sec-prob-discr-distr.

Il richiamo più importante è il concetto di **funzione di densita**: nel caso continuo, la probabilità di un intervallo è un'area sotto la curva.
:::

::: {.callout-caution collapse="true" title="Preparazione del Notebook"}

```{r}
here::here("code", "_common.R") |>
  source()

# Riapplica il tema per sicurezza, se definito nel progetto.
apply_visual_theme()
```
:::

## Che cosa descrive una distribuzione continua? {#sec-continuous-distribution-meaning}

Una distribuzione continua descrive una variabile casuale che può assumere valori in un intervallo della retta reale. La distribuzione è rappresentata da una **funzione di densità** $f(x)$.

La densità deve soddisfare due condizioni:

$$
f(x) \geq 0,
\qquad
\int_{-\infty}^{+\infty} f(x)\,dx = 1.
$$

La probabilità che $X$ cada in un intervallo $[a,b]$ è:

$$
P(a \leq X \leq b) = \int_a^b f(x)\,dx.
$$

::: {.callout-important title="Densita non significa probabilita puntuale"}
Per una variabile continua, il valore $f(x)$ non è una probabilità. Può anche essere maggiore di 1. La probabilità è sempre un'area sotto la curva, cioè l'integrale della densità su un intervallo.

Questa distinzione sarà importante nel capitolo sulla verosimiglianza: una densità può essere usata per confrontare valori del parametro, ma non deve essere confusa con una probabilità assegnata a un singolo punto.
:::

### Supporto della distribuzione

Il **supporto** è l'insieme dei valori che una variabile può assumere. Alcuni esempi:

| Distribuzione | Supporto | Uso tipico |
|---|---:|---|
| Uniforme$(a,b)$ | $[a,b]$ | parametro limitato a un intervallo |
| Normale$(\mu,\sigma^2)$ | $\mathbb{R}$ | misure simmetriche, errori additivi, effetti |
| Esponenziale$(\lambda)$ | $(0,\infty)$ | tempi di attesa con tasso costante |
| Gamma$(\alpha,\beta)$ | $(0,\infty)$ | tempi, tassi, precisioni |
| Beta$(\alpha,\beta)$ | $(0,1)$ | probabilita e proporzioni |
| $t_\nu$ | $\mathbb{R}$ | effetti continui con code pesanti |
| Cauchy$(x_0,s)$ | $\mathbb{R}$ | prior robuste molto deboli |

La prima domanda pratica, quando scegliamo una distribuzione, è quindi: **Quali valori sono possibili per la variabile o per il parametro?**

## Le funzioni R per le distribuzioni continue {#sec-r-continuous-functions}

R usa la stessa convenzione vista per le distribuzioni discrete. Ogni famiglia ha quattro funzioni principali:

| Prefisso | Significato | Esempio con la normale |
|---|---|---|
| `d` | densita | `dnorm(x, mean, sd)` |
| `p` | probabilita cumulata | `pnorm(x, mean, sd)` |
| `q` | quantile | `qnorm(p, mean, sd)` |
| `r` | generazione casuale | `rnorm(n, mean, sd)` |

Esempio: consideriamo punteggi standardizzati approssimativamente distribuiti come

$$
X \sim \mathcal{N}(100, 15^2).
$$

```{r}
mu <- 100
sigma <- 15

# Densita nel punto 100: non e una probabilita puntuale.
densita_100 <- dnorm(100, mean = mu, sd = sigma)

# Probabilita di osservare un punteggio tra 85 e 115.
prob_85_115 <- pnorm(115, mean = mu, sd = sigma) -
  pnorm(85, mean = mu, sd = sigma)

# Quantile che lascia sotto di se il 95% della distribuzione.
q_95 <- qnorm(0.95, mean = mu, sd = sigma)

# Cinque valori simulati.
simulazioni <- rnorm(5, mean = mu, sd = sigma)

densita_100
prob_85_115
q_95
simulazioni
```

::: {.callout-note title="Una lettura bayesiana delle simulazioni"}
Quando usiamo funzioni come `rnorm()` o `rgamma()`, non stiamo solo producendo numeri casuali. Stiamo simulando dati o parametri **sotto un modello**. Questa abitudine sarà utile nei capitoli successivi: simulare da una prior, da una distribuzione predittiva o da una posterior permette di rendere visibile l'incertezza.
:::

## Distribuzione uniforme continua {#sec-cont-uniform}

### Definizione

La distribuzione uniforme continua assegna la stessa densità a tutti i valori di un intervallo finito $[a,b]$.

::: {#def-uniform-cont}
Una variabile casuale $X$ segue una distribuzione uniforme continua su $[a,b]$ se

$$
X \sim \mathrm{Uniforme}(a,b),
\qquad
f(x) = \frac{1}{b-a}, \quad a \leq x \leq b,
$$
mentre $f(x)=0$ fuori dall'intervallo.

Le proprietà principali sono:

$$
\mathbb{E}(X) = \frac{a+b}{2},
\qquad
\mathbb{V}(X) = \frac{(b-a)^2}{12}.
$$
:::

### Interpretazione

La distribuzione uniforme rappresenta una forma di **indifferenza locale**: dato un intervallo, nessun sottointervallo di uguale lunghezza è ritenuto più plausibile di un altro.

Ad esempio, se prima di osservare dati riteniamo plausibile che una probabilità possa assumere qualunque valore tra 0 e 1, possiamo usare

$$
\theta \sim \mathrm{Uniforme}(0,1).
$$

Questa scelta coincide con una distribuzione $\mathrm{Beta}(1,1)$.

::: {.callout-warning title="La prior uniforme non e sempre neutrale"}
Una prior uniforme può sembrare non informativa, ma non è automaticamente neutrale. La sua forma dipende dalla scala scelta. Una prior uniforme su $\theta$ non corrisponde a una prior uniforme su una trasformazione non lineare, come $\log(\theta)$ o $\theta^2$.

Inoltre, una distribuzione uniforme su un intervallo finito esclude a priori tutti i valori fuori da quell'intervallo. Questa puo essere una scelta ragionevole solo se il vincolo è davvero parte del problema.
:::

### Probabilita come lunghezza relativa

Per $X \sim \mathrm{Uniforme}(a,b)$,

$$
P(c \leq X \leq d) = \frac{d-c}{b-a},
\qquad a \leq c < d \leq b.
$$

```{r}
# Esempio: uno spinner su un intervallo da 0 a 360 gradi.
prob <- punif(250, min = 0, max = 360) -
  punif(150, min = 0, max = 360)

prob
```

```{r}
x <- seq(0, 360, length.out = 500)
df_unif <- data.frame(
  x = x,
  densita = dunif(x, min = 0, max = 360),
  area = x >= 150 & x <= 250
)

ggplot(df_unif, aes(x = x, y = densita)) +
  geom_line(linewidth = 1) +
  geom_area(
    data = subset(df_unif, area),
    alpha = 0.4
  ) +
  labs(
    title = "Distribuzione uniforme continua",
    subtitle = "Area evidenziata: P(150 <= X <= 250)",
    x = "Valore",
    y = "Densita"
  )
```

## Distribuzione normale {#sec-cont-normal}

### Definizione

La distribuzione normale, o gaussiana, è una delle distribuzioni continue più importanti. È adatta a rappresentare misure che variano attorno a un centro e che hanno deviazioni approssimativamente simmetriche.

::: {#def-normal}
Una variabile casuale $Y$ segue una distribuzione normale con media $\mu$ e deviazione standard $\sigma > 0$ se

$$
Y \sim \mathcal{N}(\mu,\sigma^2),
$$
con densità

$$
f(y) = \frac{1}{\sigma\sqrt{2\pi}}
\exp\left[-\frac{(y-\mu)^2}{2\sigma^2}\right].
$$

Le proprietà principali sono:

$$
\mathbb{E}(Y)=\mu,
\qquad
\mathbb{V}(Y)=\sigma^2.
$$
:::

### Interpretazione dei parametri

- $\mu$ è il centro della distribuzione: valori maggiori di $\mu$ spostano la curva verso destra, valori minori verso sinistra.
- $\sigma$ è la scala della variabilita: valori maggiori producono curve piu larghe e meno concentrate.

In psicologia, la normale è spesso un modello utile per punteggi standardizzati, errori di misura, effetti continui e medie campionarie. Va però usata come **approssimazione**, non come descrizione automatica di qualunque fenomeno psicologico.

::: {.callout-note title="Perche la normale e cosi importante?"}
La normale compare spesso per due ragioni diverse:

1. può essere un modello ragionevole per dati continui generati dalla somma di molte piccole influenze indipendenti;
2. nel @sec-prob-inference-uncertainty vedremo che, sotto condizioni generali, la media campionaria tende ad avere una distribuzione approssimativamente normale.

La seconda ragione è legata al Teorema del Limite Centrale e sarà discussa nel capitolo successivo.
:::

### Regola 68-95-99.7

Per una distribuzione normale:

- circa il 68% della massa si trova entro $\mu \pm 1\sigma$;
- circa il 95% entro $\mu \pm 1.96\sigma$;
- circa il 99.7% entro $\mu \pm 3\sigma$.

```{r}
mu <- 100
sigma <- 15

x <- seq(mu - 4 * sigma, mu + 4 * sigma, length.out = 1000)
df_norm <- data.frame(
  x = x,
  densita = dnorm(x, mean = mu, sd = sigma),
  centrale_95 = x >= mu - 1.96 * sigma & x <= mu + 1.96 * sigma
)

ggplot(df_norm, aes(x = x, y = densita)) +
  geom_line(linewidth = 1) +
  geom_area(
    data = subset(df_norm, centrale_95),
    alpha = 0.4
  ) +
  geom_vline(xintercept = mu - 1.96 * sigma, linetype = "dashed") +
  geom_vline(xintercept = mu + 1.96 * sigma, linetype = "dashed") +
  labs(
    title = "Distribuzione normale e intervallo centrale al 95%",
    subtitle = "Esempio: N(100, 15^2)",
    x = "Valore",
    y = "Densita"
  )
```

### Quando la normale non e appropriata

La normale ha supporto su tutta la retta reale. Questo può essere problematico per variabili che sono vincolate:

- tempi di risposta e durate, che sono sempre positivi e spesso asimmetrici;
- conteggi di errori o sintomi, che sono discreti;
- proporzioni, che sono limitate tra 0 e 1;
- scale Likert con pochi livelli, che sono ordinali e limitate.

::: {.callout-warning title="Non normalizzare tutto"}
La normale è comoda, ma non deve diventare una scelta automatica. Una buona modellazione parte dalla natura del dato: supporto, simmetria, presenza di code pesanti, vincoli inferiori o superiori, e significato psicologico della variabile.
:::

## Distribuzione esponenziale {#sec-cont-exponential}

### Definizione

La distribuzione esponenziale modella il tempo di attesa fino al verificarsi di un evento quando il tasso di occorrenza è costante nel tempo.

::: {#def-exponential}
Una variabile casuale $T$ segue una distribuzione esponenziale con tasso $\lambda > 0$ se

$$
T \sim \mathrm{Exp}(\lambda),
\qquad
f(t) = \lambda e^{-\lambda t}, \quad t \geq 0.
$$

Le proprietà principali sono:

$$
\mathbb{E}(T)=\frac{1}{\lambda},
\qquad
\mathbb{V}(T)=\frac{1}{\lambda^2}.
$$
:::

Il parametro $\lambda$ è un **tasso**: eventi attesi per unità di tempo. Il suo inverso, $1/\lambda$, è il tempo medio di attesa.

### Assenza di memoria

La caratteristica distintiva della distribuzione esponenziale è la proprieta di **assenza di memoria**:

$$
P(T > s+t \mid T > s) = P(T > t).
$$

In altre parole: aver già atteso fino a $s$ non modifica la distribuzione dell'attesa residua. È una proprietà molto restrittiva; l'esponenziale è dunque appropriata solo quando il tasso costante è un'assunzione plausibile.

### Esempio: tempo di attesa

Supponiamo che il tempo medio di attesa per un evento sia 4 giorni. Allora $\lambda = 1/4 = 0.25$.

```{r}
lambda <- 0.25

# Probabilita di attendere al massimo 1.5 giorni.
pexp(1.5, rate = lambda)

# Probabilita di attendere tra 1 e 6 giorni.
pexp(6, rate = lambda) - pexp(1, rate = lambda)
```

```{r}
x <- seq(0, 20, length.out = 500)
df_exp <- data.frame(
  x = x,
  densita = dexp(x, rate = lambda),
  area = x >= 1 & x <= 6
)

ggplot(df_exp, aes(x = x, y = densita)) +
  geom_line(linewidth = 1) +
  geom_area(data = subset(df_exp, area), alpha = 0.4) +
  labs(
    title = "Distribuzione esponenziale",
    subtitle = "Area evidenziata: P(1 <= T <= 6)",
    x = "Tempo di attesa",
    y = "Densita"
  )
```

::: {.callout-note title="Applicazioni psicologiche"}
L'esponenziale può essere utile per modellare le durate o i tempi tra gli eventi quando il rischio istantaneo è approssimativamente costante, ad esempio il tempo necessario per distrarsi, l'intervallo tra i comportamenti osservabili o il tempo necessario perché si verifichi un evento in un compito sperimentale.

Per i tempi di reazione, tuttavia, l'esponenziale è spesso troppo semplice: distribuzioni come la log-normale, la gamma o l'ex-Gaussiana possono essere più realistiche.
:::

## Distribuzione gamma {#sec-cont-gamma}

### Definizione

La distribuzione gamma è una famiglia flessibile per le variabili positive. Può essere utilizzata per modellare tempi, durate, quantità strettamente positive, tassi e precisioni.

In R useremo la parametrizzazione **shape-rate**.

::: {#def-gamma}
Una variabile casuale $X$ segue una distribuzione gamma con parametri $\alpha > 0$ e $\beta > 0$ se

$$
X \sim \mathrm{Gamma}(\alpha,\beta),
$$
con densità

$$
f(x) = \frac{\beta^\alpha}{\Gamma(\alpha)} x^{\alpha-1} e^{-\beta x},
\qquad x > 0.
$$

Le sue proprietà principali sono:

$$
\mathbb{E}(X)=\frac{\alpha}{\beta},
\qquad
\mathbb{V}(X)=\frac{\alpha}{\beta^2}.
$$
:::

Il parametro $\alpha$ controlla la forma della distribuzione, mentre il parametro $\beta$ controlla il tasso di decadimento. In R si usa `dgamma(x, shape = alpha, rate = beta)`.

::: {.callout-warning title="Rate e scale"}
Spesso, la gamma è parametrizzata anche con un parametro di scala $\theta = 1/\beta$. In R, l'argomento `rate` usa $\beta$, mentre l'argomento `scale` usa $1/\beta$. Prima di utilizzare R è necessario verificare la parametrizzazione.
:::

### Relazione con l'esponenziale

L'esponenziale è un caso particolare della gamma:

$$
\mathrm{Exp}(\lambda) = \mathrm{Gamma}(1,\lambda).
$$

In un processo a tasso costante, la gamma esprime il tempo necessario ad accumulare più eventi. Se $\alpha$ è intero, la gamma con parametro di forma $\alpha$ descrive l'attesa fino al verificarsi di $\alpha$ eventi.

### Ruolo bayesiano

Nel percorso bayesiano, la gamma è importante per parametri positivi:

- come prior per un tasso $\lambda > 0$;
- come prior per una precisione $\tau = 1/\sigma^2$;
- come modello per durate positive e asimmetriche.

La distribuzione gamma è la coniugata naturale del tasso \(\lambda\) della Poisson. Se nel capitolo precedente abbiamo scritto

$$
Y_i \mid \lambda \sim \mathrm{Poisson}(\lambda),
$$

allora una prior gamma su $\lambda$ genera una posterior ancora gamma. Tornerà utile quando affronteremo più esplicitamente l'aggiornamento bayesiano.

### Flessibilità della forma

```{r}
x <- seq(0, 10, length.out = 500)
params_gamma <- data.frame(
  alpha = c(1, 2, 4, 9),
  beta = c(1, 1, 1, 2)
)

df_gamma <- do.call(rbind, lapply(seq_len(nrow(params_gamma)), function(i) {
  alpha <- params_gamma$alpha[i]
  beta <- params_gamma$beta[i]
  data.frame(
    x = x,
    densita = dgamma(x, shape = alpha, rate = beta),
    distribuzione = paste0("Gamma(", alpha, ", ", beta, ")")
  )
}))

ggplot(df_gamma, aes(x = x, y = densita, color = distribuzione)) +
  geom_line(linewidth = 1) +
  labs(
    title = "Flessibilita della distribuzione gamma",
    x = "x",
    y = "Densita",
    color = "Distribuzione"
  )
```

::: {.callout-tip collapse="true" title="Anticipazione: aggiornamento Gamma-Poisson"}
Se $\lambda \sim \mathrm{Gamma}(\alpha_0,\beta_0)$ e osserviamo conteggi Poisson indipendenti $y_1,\ldots,y_n$, allora

$$
\lambda \mid y \sim
\mathrm{Gamma}\left(\alpha_0 + \sum_{i=1}^n y_i,\; \beta_0+n\right).
$$

Questa formula non è ancora il tema centrale del capitolo, ma serve solo a mostrare perché la gamma è una distribuzione così utile nel vocabolario bayesiano.
:::

## Distribuzione beta {#sec-cont-beta}

### Definizione

La distribuzione beta è definita sull'intervallo $(0,1)$. Per questo motivo, è la scelta naturale per rappresentare l'incertezza su probabilità, proporzioni e parametri vincolati tra 0 e 1.

::: {#def-beta}
Una variabile casuale $\theta$ segue una distribuzione beta con parametri $\alpha > 0$ e $\beta > 0$ se

$$
\theta \sim \mathrm{Beta}(\alpha,\beta),
$$
con densità

$$
f(\theta) = \frac{\theta^{\alpha-1}(1-\theta)^{\beta-1}}{B(\alpha,\beta)},
\qquad 0 < \theta < 1.
$$

Le proprietà principali sono:

$$
\mathbb{E}(\theta)=\frac{\alpha}{\alpha+\beta},
$$

$$
\mathbb{V}(\theta)=
\frac{\alpha\beta}{(\alpha+\beta)^2(\alpha+\beta+1)}.
$$
:::

### Interpretazione dei parametri

La beta è particolarmente intuitiva in un contesto bayesiano. I parametri possono essere letti come una forma di informazione preliminare:

- $\alpha$ è collegato alla quantità di evidenza a favore dell'evento;
- $\beta$ è collegato alla quantità di evidenza contro l'evento;
- $\alpha + \beta$ rappresenta la forza complessiva della distribuzione.

Una distribuzione $\mathrm{Beta}(1,1)$ è uniforme su $(0,1)$. Una $\mathrm{Beta}(20,10)$ concentra la massa attorno a valori più alti, come se l'informazione preliminare suggerisse più successi che insuccessi.

::: {.callout-note title="Pseudo-osservazioni: una convenzione utile"}
Spesso, si interpreta $\alpha-1$ come il numero di pseudo-successi e $\beta-1$ come il numero di pseudo-insuccessi. In altri contesti, invece, si usano $\alpha$ e $\beta$ come conteggi effettivi. L'importante è non confondere la metafora con una regola universale: la distribuzione beta rappresenta una distribuzione di credenze, non necessariamente un vero campione precedente.
:::

### Ruolo bayesiano

La distribuzione beta è la prior coniugata naturale per il parametro di una distribuzione di Bernoulli o binomiale:

$$
Y \mid \theta \sim \mathrm{Binomiale}(n,\theta),
\qquad
\theta \sim \mathrm{Beta}(\alpha,\beta).
$$

Dopo aver osservato $y$ successi su $n$ prove, la distribuzione a posteriori è:

$$
\theta \mid y \sim \mathrm{Beta}(\alpha+y,\beta+n-y).
$$

Nel capitolo 13 useremo proprio questo tipo di modello per collegare la proporzione campionaria, l'errore standard e l'informazione nei dati.

### Forme possibili

```{r}
x <- seq(0.001, 0.999, length.out = 500)
params_beta <- data.frame(
  alpha = c(0.5, 1, 2, 2, 5, 20),
  beta = c(0.5, 1, 2, 5, 2, 10)
)

df_beta <- do.call(rbind, lapply(seq_len(nrow(params_beta)), function(i) {
  alpha <- params_beta$alpha[i]
  beta <- params_beta$beta[i]
  data.frame(
    x = x,
    densita = dbeta(x, shape1 = alpha, shape2 = beta),
    distribuzione = paste0("Beta(", alpha, ", ", beta, ")")
  )
}))

ggplot(df_beta, aes(x = x, y = densita, color = distribuzione)) +
  geom_line(linewidth = 1) +
  labs(
    title = "Flessibilita della distribuzione beta",
    subtitle = "La beta puo rappresentare ignoranza, preferenze moderate o credenze concentrate",
    x = expression(theta),
    y = "Densita",
    color = "Distribuzione"
  )
```

## Distribuzione chi-quadrato {#sec-cont-chisq}

### Definizione

La distribuzione chi-quadrato nasce dalla somma di quadrati di variabili normali standard indipendenti.

::: {#def-chisq}
Se

$$
Z_1,\ldots,Z_k \stackrel{iid}{\sim} \mathcal{N}(0,1),
$$
allora
$$
X = Z_1^2 + \cdots + Z_k^2 \sim \chi^2_k.
$$

Il parametro $k$ è il numero di gradi di libertà. Le proprietà principali sono:

$$
\mathbb{E}(X)=k,
\qquad
\mathbb{V}(X)=2k.
$$
:::

### Interpretazione

La chi-quadrato misura una dispersione quadratica: somma le deviazioni standardizzate, le eleva al quadrato e quindi considera la grandezza complessiva dello scostamento, a prescindere dal segno.

Si tratta di un caso particolare della funzione gamma:

$$
\chi^2_k = \mathrm{Gamma}\left(\frac{k}{2},\frac{1}{2}\right)
$$
nella parametrizzazione shape-rate.

### Ruolo bayesiano

La chi-quadrato è importante perché compare nella teoria della varianza campionaria e, indirettamente, nei modelli normali con varianza sconosciuta. Nelle formulazioni bayesiane moderne si lavora spesso con distribuzioni gamma o inverse-gamma per varianze e precisioni, ma la chi-quadrato rimane un ponte concettuale utile.

```{r}
x <- seq(0, 40, length.out = 500)
df_chisq <- data.frame(
  x = rep(x, 4),
  densita = c(dchisq(x, df = 2), dchisq(x, df = 4),
              dchisq(x, df = 8), dchisq(x, df = 16)),
  df = rep(c("2", "4", "8", "16"), each = length(x))
)

ggplot(df_chisq, aes(x = x, y = densita, color = df)) +
  geom_line(linewidth = 1) +
  labs(
    title = "Distribuzione chi-quadrato",
    subtitle = "All'aumentare dei gradi di liberta la distribuzione diventa meno asimmetrica",
    x = "x",
    y = "Densita",
    color = "Gradi di liberta"
  )
```

## Distribuzione t di Student {#sec-cont-student-t}

### Definizione

La distribuzione $t$ di Student emerge quando una variabile normale viene standardizzata utilizzando una stima incerta della sua varianza.

::: {#def-student-t}
Se $Z \sim \mathcal{N}(0,1)$, $W \sim \chi^2_\nu$ e $Z$ e $W$ sono indipendenti, allora

$$
T = \frac{Z}{\sqrt{W/\nu}} \sim t_\nu.
$$

Il parametro $\nu$ rappresenta i gradi di libertà.
:::

### Interpretazione

La $t$ assomiglia alla normale, ma ha **code più pesanti**, soprattutto quando $\nu$ e piccolo. Ciò significa che assegna maggiore plausibilità a valori lontani dal centro.

Quando $\nu$ aumenta, la $t$ tende alla distribuzione normale standard.

### Ruolo bayesiano

La $t$ è utile in due modi:

1. come modello o distribuzione predittiva quando la scala è incerta;
2. come prior debolmente informativa e robusta per parametri continui.

Nel lavoro applicato, l'uso di una distribuzione $t$ al posto di una normale può ridurre la sensibilità del modello ai valori estremi.

```{r}
x <- seq(-5, 5, length.out = 500)
df_t <- data.frame(
  x = rep(x, 4),
  densita = c(dnorm(x), dt(x, df = 1), dt(x, df = 3), dt(x, df = 10)),
  distribuzione = rep(c("Normale", "t(1)", "t(3)", "t(10)"), each = length(x))
)

ggplot(df_t, aes(x = x, y = densita, color = distribuzione)) +
  geom_line(linewidth = 1) +
  labs(
    title = "Confronto tra normale e t di Student",
    subtitle = "Con pochi gradi di liberta, la t ha code piu pesanti",
    x = "x",
    y = "Densita",
    color = "Distribuzione"
  )
```

## Distribuzione di Cauchy {#sec-cont-cauchy}

### Definizione

La distribuzione di Cauchy è un caso particolare della distribuzione $t$ con un solo grado di libertà.

::: {#def-cauchy}
Una variabile casuale $X$ segue una distribuzione di Cauchy con parametro di posizione $x_0$ e parametro di scala $s>0$ se

$$
X \sim \mathrm{Cauchy}(x_0,s),
$$
con densità
$$
f(x)=\frac{1}{\pi s\left[1+\left(\frac{x-x_0}{s}\right)^2\right]}.
$$
:::

La distribuzione di Cauchy ha code molto pesanti e non possiede media e varianza finite. Il parametro $x_0$ indica una posizione centrale, ma non è una media nel senso usuale.

### Ruolo bayesiano

La distribuzione di Cauchy è spesso utilizzata come prior robusta e debolmente informativa per i parametri di posizione o i coefficienti di regressione. La sua versione troncata ai valori positivi, detta *half-Cauchy*, è usata come prior per i parametri di scala, ad esempio le deviazioni standard nei modelli gerarchici.

::: {.callout-warning title="Code pesanti non significa assenza di assunzioni"}
Una prior Cauchy lascia molto spazio ai valori estremi. Questo può essere utile per la robustezza, ma può anche rendere l'inferenza instabile se i dati sono pochi o il modello è debole. Una prior robusta non sostituisce il ragionamento sostanziale sulla scala plausibile degli effetti.
:::

```{r}
x <- seq(-10, 10, length.out = 800)
df_cauchy <- data.frame(
  x = rep(x, 3),
  densita = c(dnorm(x), dt(x, df = 3), dcauchy(x)),
  distribuzione = rep(c("Normale(0,1)", "t(3)", "Cauchy(0,1)"), each = length(x))
)

ggplot(df_cauchy, aes(x = x, y = densita, color = distribuzione)) +
  geom_line(linewidth = 1) +
  labs(
    title = "Normale, t e Cauchy",
    subtitle = "La Cauchy assegna molta piu massa alle code",
    x = "x",
    y = "Densita",
    color = "Distribuzione"
  )
```

## Come scegliere una distribuzione continua? {#sec-cont-distribution-choice}

La scelta di una distribuzione non dovrebbe basarsi sulla convenienza matematica, ma sulla struttura del problema. Tre domande aiutano a orientarsi:

### 1. Qual è il supporto?

- Se la quantità può assumere qualsiasi valore reale, le candidate sono la normale, la $t$ o la Cauchy.
- Se è positiva, si considerano l'esponenziale, la gamma o la log-normale.
- Se è vincolata tra 0 e 1, la beta è spesso la scelta naturale.
- Se invece è un conteggio, non siamo nel caso continuo: si torna alla Poisson, alla binomiale o ad altre distribuzioni discrete.

### 2. Che forma ci aspettiamo?

- Simmetria attorno a un centro: normale o $t$.
- Asimmetria positiva: gamma o log-normale.
- Probabilità/proporzioni vincolate: beta.
- Code molto pesanti: $t$ con pochi gradi di libertà.

### 3. Che ruolo ha la distribuzione?

La stessa famiglia può avere ruoli diversi, ad esempio:

- la normale può essere un modello per dati continui o una prior per un effetto;
- la beta è spesso una prior per una probabilità, ma può anche modellare dati continui proporzionali;
- la gamma può modellare tempi positivi oppure essere una prior per un tasso;
- la $t$ può essere un modello robusto per dati o una prior robusta per parametri.

::: {.callout-important title="Modello generativo prima dell'inferenza"}
Prima di chiedersi come aggiornare una credenza, occorre chiarire quale modello collega il parametro ai dati osservabili. Questo capitolo fornisce il vocabolario delle distribuzioni continue; il @sec-prob-inference-uncertainty mostrerà come un campione generato da questi modelli risulti più o meno informativo.
:::

## Riflessioni conclusive {.unnumbered .unlisted}

Le distribuzioni continue completano il vocabolario avviato con quelle discrete, consentendo di descrivere dati e parametri che si collocano su scale continue: punteggi, durate, tassi, probabilità, effetti e grandezze positive.

La prospettiva bayesiana aiuta a mantenere distinti i ruoli:

- una distribuzione può descrivere **dati osservabili condizionatamente a un parametro**;
- una distribuzione può rappresentare **incertezza epistemica su un parametro**;
- una volta osservati i dati, lo stesso modello può diventare una **funzione di verosimiglianza**.

Questa distinzione previene una confusione comune: la beta, ad esempio, non è di solito un modello per una sequenza di risposte corrette/errate, bensì una distribuzione su una probabilità. La binomiale descrive i conteggi osservati; la beta, l'incertezza sul parametro che li genera.

Nel prossimo capitolo useremo questo vocabolario per affrontare una nuova domanda: **quanto è informativo un campione?** Vedremo che media campionaria, errore standard, precisione, Legge dei Grandi Numeri e Teorema del Limite Centrale spiegano perché i dati possono stabilizzare o modificare le nostre credenze.

## Punti chiave da ricordare {.unnumbered}

::: {.callout-important}

1. **Nel caso continuo, le probabilita sono aree**
   - Per una variabile continua, in genere $P(X=x)=0$.
   - La densita $f(x)$ non e una probabilita puntuale.
   - $P(a \leq X \leq b)=\int_a^b f(x)\,dx$.

2. **Il supporto guida la scelta del modello**
   - $\mathbb{R}$: normale, $t$, Cauchy.
   - $(0,\infty)$: esponenziale, gamma.
   - $(0,1)$: beta.

3. **La normale e centrale, ma non universale**
   - Utile per errori additivi, punteggi standardizzati, effetti continui.
   - Non adatta automaticamente a tempi, conteggi, proporzioni o scale ordinali.

4. **La gamma e utile per quantita positive**
   - Include l'esponenziale come caso particolare.
   - E naturale per tassi, durate, precisioni e processi positivi.

5. **La beta rappresenta incertezza su probabilita**
   - Supporto $(0,1)$.
   - Coniugata con Bernoulli e binomiale.
   - Essenziale per inferenza bayesiana su proporzioni.

6. **Le distribuzioni a code pesanti aumentano la robustezza**
   - La $t$ e piu robusta della normale.
   - La Cauchy e ancora piu permissiva nelle code.
   - Robustezza non significa assenza di assunzioni.

7. **Le distribuzioni sono strumenti modellistici**
   - Non sono fotografie della realta.
   - Sono modi espliciti per rappresentare assunzioni su dati, parametri e incertezza.

**Formule da ricordare**

Normale:

$$
X \sim \mathcal{N}(\mu,\sigma^2),
\qquad
\mathbb{E}(X)=\mu,
\qquad
\mathbb{V}(X)=\sigma^2.
$$

Gamma, parametrizzazione shape-rate:

$$
X \sim \mathrm{Gamma}(\alpha,\beta),
\qquad
\mathbb{E}(X)=\frac{\alpha}{\beta},
\qquad
\mathbb{V}(X)=\frac{\alpha}{\beta^2}.
$$

Beta:

$$
\theta \sim \mathrm{Beta}(\alpha,\beta),
\qquad
\mathbb{E}(\theta)=\frac{\alpha}{\alpha+\beta}.
$$

Esponenziale:

$$
T \sim \mathrm{Exp}(\lambda),
\qquad
\mathbb{E}(T)=\frac{1}{\lambda}.
$$

**Per il prossimo capitolo**

Nel @sec-prob-inference-uncertainty useremo le distribuzioni appena studiate per capire come un campione produce evidenza: perché le medie campionarie fluttuano, in che modo l'errore standard misura la precisione e in che senso i campioni più grandi rendono l'evidenza più stabile.
:::

::: {.callout-note collapse="true" title="Informazioni sull'ambiente di sviluppo"}

```{r}
sessionInfo()
```
:::

## Bibliografia {.unnumbered .unlisted}
